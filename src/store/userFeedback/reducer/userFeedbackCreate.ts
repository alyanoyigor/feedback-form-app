import { createSlice } from '@reduxjs/toolkit';

const USER_FEEDBACK_CREATE_SLICE_NAME = 'USER_FEEDBACK_CREATE_SLICE';

const userFeedbackCreateSlice = createSlice({
  name: USER_FEEDBACK_CREATE_SLICE_NAME,
  initialState: {},
  reducers: {},
});

export default userFeedbackCreateSlice.reducer;
